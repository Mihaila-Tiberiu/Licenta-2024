import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';

const cities = ['Bucuresti', 'Brasov', 'Cluj-Napoca', 'Timisoara', 'Oradea'];

function CityCarousel() {
  return(<div></div>);
  
}

export default CityCarousel;
